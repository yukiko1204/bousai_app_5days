# 避難所検索画面を再現するための一括実装プロンプト

以下の要件をすべて満たすように、既存システムを確認してから実装してください。質問を返さず、既存の構成・フレームワーク・デザイン規約に合わせて、必要なファイルの作成・変更・テストまで一度に完了してください。既存機能や無関係な画面は壊さないでください。

## 目的

避難所検索画面を作成または改善する。画面上部に実際の地図を表示し、青森市内の避難所を赤いピンと避難所名で表示する。ブラウザ利用者の現在地は青い丸で表示する。

## 必須機能

### 1. 青森市の町名選択

- 選択バーの選択肢は青森市の町名だけにする。
- 他の市区町村、都道府県、架空の地域名は選択肢に入れない。
- 町名は五十音順のグループに分けて表示する。
- 町名ごとに読み仮名も表示する。
- 初期値は「選択しない」にする。
- 選択した町名で検索できるようにする。
- サーバー側でも町名を検証し、未許可の値を検索条件として処理しない。

青森市の町名データ例:

- あ行: 青葉、青柳、旭町、浅虫、石江、泉野、一本木、今井、後潟、内真部、浦町、大野、大谷、岡造道、沖館、奥野、小館、小柳、卸町
- か行: 勝田、桂木、金沢、金浜、合浦、北金沢、久須志、久栗坂、幸畑
- さ行: 栄町、桜川、里見、三内、篠田、清水、自由ケ丘、新町、新田、新城、諏訪沢
- た行: 千刈、千富町、第二問屋町、茶屋町、月見野、堤町、佃、造道、筒井、鶴ケ坂、戸門、戸崎、富田
- な行: 長島、中佃、浪打、浪館、西大野、西滝、野木、野内
- は行: 橋本、浜田、浜館、原別、東大野、東造道、左堰、古川、本町
- ま行: 松原、松森、三好、港町、南佃、妙見、本泉
- や行: 八重田、矢作、安方、山手、横内、四ツ石
- ら行: 流通団地
- わ行: 若葉、若松

### 2. 実際の地図

- 疑似的なCSS図や固定の背景図ではなく、実際の地図を使う。
- 既存プロジェクトに地図ライブラリがある場合はそれを使う。
- ない場合はLeaflet 1.9.4とOpenStreetMapタイルを使う。
- LeafletのCSSとJavaScriptを読み込む。
- 地図の初期中心は青森市中心部、例として緯度40.8220、経度140.7474付近にする。
- デスクトップでは高さ約320px、モバイルでは高さ約230px程度にする。
- 地図提供元の帰属表示を必ず表示する。

### 3. 避難所の表示

避難所データは次のような形式で持つ。既存のデータ形式がある場合は、それに合わせて必要な座標フィールドを追加する。

```json
[
  {
    "id": 1,
    "name": "避難所名",
    "latitude": 40.8244,
    "longitude": 140.7406
  }
]
```

- 全避難所を緯度・経度に基づいて地図へ表示する。
- 避難所は赤色のピンで表示する。
- ピンの横または上に避難所名を表示する。
- 避難所名は、可能なら常時表示するラベルにする。
- 座標がない避難所は、架空の位置を勝手に作らず、登録画面やデータ管理側で座標を登録できるようにする。座標がないデータを表示しない場合は、その理由が分かる状態にする。
- 避難所データをテンプレートやフロントエンドへ渡す際は、JSONとして安全にエスケープする。

### 4. 利用者の現在地

- `navigator.geolocation.getCurrentPosition` または既存システムの位置情報APIを利用する。
- ページ読み込み時に現在地の取得を試みる。
- 現在地は地図上に青い円として表示する。
- 現在地の精度を取得できる場合は、薄い青色の精度円も表示する。
- 現在地取得後は地図の中心を現在地へ移動し、適切なズームにする。
- 地図上に現在地を再取得するボタンを配置する。ボタンは位置情報アイコンを使い、マウス操作とキーボード操作に対応させる。
- 位置情報が拒否された、タイムアウトした、または取得できない場合でも、地図と避難所ピンは表示する。
- 取得失敗時は、視覚的に邪魔にならないアクセシブルなステータスへ「位置情報を許可してください」などのメッセージを設定する。
- 現在地はHTTPSまたはlocalhostなど、ブラウザが位置情報を許可する環境で動作することを明記する。

現在地マーカーの見た目の例:

- 塗り: `#1976d2`
- 外周: 白色
- 精度円: 青色、透明度0.16程度

### 5. 検索操作のレイアウト

PCの全画面表示では、添付イメージのように次の順序で配置する。

1. 青森市の町名選択バーと検索ボタンを同じ行に配置
2. その直下中央に「空きのある施設だけ表示」ボタンまたはリンクを配置
3. さらにその下の中央に「施設の条件」を配置

- 選択バーと検索ボタンは横幅を揃え、操作しやすい高さにする。
- 検索操作全体は画面中央に配置する。
- 「空きのある施設だけ表示」は検索行の下に置く。
- 施設条件は左寄りにせず、画面中央に配置する。
- モバイルでは選択バー、検索ボタン、空き施設ボタンを縦方向に並べる。
- 既存の検索結果ページと正しく接続する。

### 6. 施設条件のデザイン

次のチェック項目を中央に配置する。

- バリアフリー対応
- ペット可
- 乳幼児対応
- 駐車場あり

デザイン要件:

- `fieldset` と `legend` を使い、アクセシビリティを保つ。
- 条件項目は中央揃えにする。
- 各項目は白背景、薄い枠線、角丸のチップまたはボタン風にする。
- チェック済みは青系の枠線と薄い青背景で分かるようにする。
- チェックボックスにはテーマカラーを適用する。
- キーボードフォーカスが見えるようにする。
- `:has()`を使う場合は、未対応ブラウザでも致命的に崩れないようにする。

### 7. デザイン

- ページの横幅は最大1080px程度、左右に適切な余白を設ける。
- 地図は薄い枠線と小さな角丸にする。
- 検索操作は薄い青系の背景、枠線、控えめな影を持つまとまりとして表示する。
- 検索ボタンは青色背景と白文字にする。
- フォーカス時は青色のアウトラインを表示する。
- 既存サイトのヘッダー、フォント、色、アイコンライブラリを尊重する。
- Font Awesomeなど既存のアイコンライブラリがあれば、位置情報や地図ピンのアイコンに利用する。
- 無関係なページのデザインや機能は変更しない。

## 実装上の注意

- まず既存のルーティング、テンプレート、データ構造、CSS、JavaScriptを確認する。
- 既存の避難所検索ページを所有するファイルを特定し、その周辺だけを変更する。
- 新しい依存関係を追加する場合は、依存関係ファイルも更新する。
- CDNを使う場合はバージョンを固定する。
- 位置情報はサーバーで取得せず、ブラウザのGeolocation APIで取得する。
- ユーザーの位置情報を保存・送信しない。
- 地図タイルの利用規約と帰属表示を守る。
- XSSを防ぐため、避難所名をHTML文字列として直接連結せず、地図ライブラリの安全なAPIやエスケープ処理を使う。
- 既存の検索条件や結果一覧を壊さない。

## 完了条件

次のすべてを満たした状態で完了すること。

- [ ] 選択バーに青森市の町名だけが表示される
- [ ] 選択バーと検索ボタンがPCでは同じ行にある
- [ ] 空き施設ボタンが検索行の下にある
- [ ] 施設条件が画面中央にある
- [ ] 実際の地図タイルが表示される
- [ ] 青森市中心部が初期表示される
- [ ] 避難所が赤いピンで表示される
- [ ] 各避難所名が地図上に表示される
- [ ] 現在地が取得できた場合、青い丸で表示される
- [ ] 現在地の精度円が表示される
- [ ] 現在地の再取得ボタンが動作する
- [ ] 位置情報を拒否しても地図と避難所ピンは表示される
- [ ] PCとモバイルの両方でレイアウトが崩れない
- [ ] 既存の検索結果遷移が動作する
- [ ] テンプレート描画、JSON読み込み、JavaScript構文、差分の空白エラーを検証する

## 検証

実装後、環境に合ったコマンドで次を実行すること。

1. バックエンドの構文チェック
2. 避難所JSONの妥当性チェック
3. 避難所検索ページのHTTPレスポンスチェック
4. 町名が青森市のデータだけであることの確認
5. HTMLに地図ライブラリ、避難所マーカー、現在地処理が含まれることの確認
6. 埋め込みJavaScriptの構文チェック
7. `git diff --check`相当の空白チェック
8. ブラウザで可能なら、位置情報を許可した場合と拒否した場合の両方を確認

最後に、変更したファイル、実装した機能、実行した検証、位置情報に必要なブラウザ条件を簡潔に報告してください。
